<div class="row">
  <div class="col-md-12">
    <h1>Behaviors</h1>

    <p>Pushbit has many different behaviors and we're always working on adding more!
    Here's the big list of everything that Pushbit can do for your team.</p>
  </div>
</div>

<div class="row" class="behaviors">
  <ul>
  <% @behaviors.each do |behavior| %>
    <li class="col-md-4">
      <section class="behavior">
        <h3><%= behavior.name %></h3>
        <% if behavior.active %>
          <p class="tags"><%= behavior.tags.length > 0 ? behavior.tags.join(', ') : "All" %></p>
        <% else %>
          <p class="tags coming-soon"><%= behavior.active ? '' : 'coming soon' %></p>
        <% end %>
        <p><%= behavior.description %></p>
      </section>
    </li>
  <% end %>
  </ul>
</div>
