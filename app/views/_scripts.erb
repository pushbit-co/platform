<script src="https://checkout.stripe.com/checkout.js"></script>

<script>
  window.STRIPE_PUBLISHABLE_KEY = "<%= ENV.fetch('STRIPE_PUBLISHABLE_KEY') %>";
  <% if current_user %>
  window.USER = {
    id: <%= current_user.id %>,
    email: "<%= current_user.email %>"
  };
  <% end %>
</script>

<%= javascript_tag 'bundle' %>
